-- Sharing metadata: read by tooling and the in-game manager detail pane,
-- never by the loader's merge.
return {
  summary = "A QUESTS entry in the START menu that tracks every quest in the pack and its status.",
  author = "chrispanella",
  contact = "https://github.com/chrispanella/gen1recomp-mods",
  tags = { "ui", "quest", "tool" },
  differences = {
    added = {
      "a QUESTS item in the start menu that opens a quest log",
      "status (not started / active / done / which ending) for the Glow Shard, the four Town Quests, and the five Branching Quests",
    },
    changed = {
      "the start menu gains one entry, above SAVE",
    },
    known = {
      "the quest list is a fixed table; add a row to track a new quest",
    },
  },
  credits = {},
  compat = { engine = ">=0.0.0-0 <2.0.0", modApi = 2 },
}
